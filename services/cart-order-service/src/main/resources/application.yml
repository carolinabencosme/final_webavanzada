server:
  port: 8083
spring:
  application:
    name: cart-order-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:orderdb}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
payment:
  mock: ${PAYMENT_MOCK:true}
paypal:
  # Local debug env:
  # - PAYPAL_ENABLED=true
  # - PAYMENT_MOCK=false (forces PayPalPaymentProvider as active provider)
  # - PAYPAL_CLIENT_ID / PAYPAL_CLIENT_SECRET set with sandbox app credentials
  # - Optional PAYPAL_HEALTH_ENABLED=true to expose /payments/paypal/health outside local/dev profiles
  enabled: ${PAYPAL_ENABLED:false}
  base-url: ${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
  client-id: ${PAYPAL_CLIENT_ID:}
  client-secret: ${PAYPAL_CLIENT_SECRET:}
  health-enabled: ${PAYPAL_HEALTH_ENABLED:false}
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
  instance:
    prefer-ip-address: true
management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_URL:http://localhost:9411/api/v2/spans}
  endpoints:
    web:
      exposure:
        include: health,info
